/** @deprecated Import `Section` from `../ui/Section`. Re-export kept for the profile barrel. */
export { default } from '../ui/Section'
export type { SectionProps as CollapsibleProfileSectionProps, SectionStatus } from '../ui/sectionTypes'
