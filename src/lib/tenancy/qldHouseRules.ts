/**
 * Re-exports the QLD rooming house-rules generator from `api/lib`.
 * Implementation lives under `api/lib` so Vercel Node bundles include it.
 */
export * from '../../../api/lib/tenancy/qldHouseRules/index.js'
