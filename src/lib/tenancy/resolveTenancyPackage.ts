/**
 * Re-exports the tenancy package router from `api/lib/resolveTenancyPackage.ts`.
 * Implementation must live under `api/lib` so Vercel serverless bundles include it.
 */
export * from '../../../api/lib/resolveTenancyPackage.js'
