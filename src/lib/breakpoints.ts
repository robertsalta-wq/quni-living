/** Tailwind `sm` — matches LandlordMobileBottomNav / app-shell `max-sm:` (640px). */
export const TAILWIND_SM_PX = 640

/** Mobile = below `sm` (max-width 639px). */
export const MOBILE_MAX_WIDTH_MQ = `(max-width: ${TAILWIND_SM_PX - 1}px)`
