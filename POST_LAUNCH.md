# Post-launch follow-ups

- AI assessment is comparing student preferences against wrong listing details — needs prompt/data fix
- "Open" link in landlord bookings table — verify it still works correctly after our dashboard changes
- Visual styling of NSW RTA document — currently plain text, could be improved to closer match government form layout
- Migration history alignment — db push will need repair before it can be used reliably
